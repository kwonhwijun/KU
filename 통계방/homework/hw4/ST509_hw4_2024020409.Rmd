---
title: "ST509_HW3_2024020409"
author: "Hwijun Kwon"
date: "2024-03-29"
output: pdf_document
---


# 1
```{r Soft Threshold Operator}
S <- function(z, lambda){
    (z-lambda) * (z>lambda) + (z+lambda) * (z<-lambda) # + 0 *(ABS) ~
}

S(z = 3,lambda = 10)
```

```{r}
cd.lasso <- function(x, y, lambda){
  # centering y and standardizing X
  z <- scale(x)
  m <- attr(z, "scaled:center")
  s <- attr(z, "scaled:sacle")
  u <- (y-mean(y))
  beta <- coef(lm(u ~ z - 1)) 
  r <- u - z %*% beta #initialization
  
  
}

```


```{r}
getwd()
```
```{r}
train <- matrix(scan("train.txt"), 500, 51)
test  <- matrix(scan("test.txt"), 500, 51)
x <- train[,-51] ; y <- train[,51]
x.test <- test[,-51]
y.test <- test[,51]
```